zuul:
  routes:
    claim-auth:
      path: /api/auth/**
      serviceId: claim-auth
    claim-admin:
      path: /api/admin/**
      serviceId: claim-admin-biz
    claim-sidecar:
      path: /api/sidecar/**
      serviceId: claim-sidecar
#    sct-monitor:
#      path: /api/monitor/**
#      serviceId: sct-monitor
#    sct-zipkin:
#      path: /api/zipkin/**
#      serviceId: sct-zipkin
#    sct-eureka:
#      path: /api/eureka/**
#      serviceId: sct-eureka

